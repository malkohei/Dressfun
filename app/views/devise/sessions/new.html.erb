<div class="background" style="height: 560px">
  <div class="sign-in rounded shadow">
    <h2 class="text-center my-3"><strong>ログインする<i class="fas fa-sign-in-alt " style="color: black; font-size: 30px;"></i></strong></h2>
      <%= render "devise/shared/error_messages", resource: resource %>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <div class="my-3 offset-md-2 col-md-8">
          <label>メールアドレス</label><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <div class="my-3 offset-md-2 col-md-8">
          <label>パスワード</label><br />
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "ログインする", class: "btn btn-success offset-md-2 col-md-8" %>
        </div>
      <% end %>
      <br />
      <p class="text-center">--新規登録がまだの方--</p>

      <%= link_to new_user_registration_path do %>
        <button type="button" class="btn btn-outline-primary offset-md-2 col-md-8 my-4">新規登録へ</button>
      <% end %>
  </div>
</div>
