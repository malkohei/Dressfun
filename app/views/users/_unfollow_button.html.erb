<%= form_for(current_user, url: relationship_path(@user), method: :delete, remote: true) do |f| %>
  <%= hidden_field_tag :following_id, @user.id %>
  <%= f.submit "フォロー解除", class: "btn btn-outline-secondary rounded-pill form-control" %>
<% end %>